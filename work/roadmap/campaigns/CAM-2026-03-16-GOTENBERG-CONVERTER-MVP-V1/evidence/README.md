# Evidence

This directory stores local proof-of-carrier evidence for the active campaign.

Expected artifacts:
- `latest-run.json` with the latest smoke conversion metadata
- generated PDF output under `example/output/`
- manual notes on rendering caveats after review

Do not treat `example/example.doc` as a permanent fixture.
It is a temporary local sample and should later be replaced by a safe anonymized fixture or by a documented local sample workflow.
